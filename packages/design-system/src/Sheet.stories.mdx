import { Button } from './Button'
import { ShadcnLabel } from './ShadcnLabel'
import {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from './Sheet'
import { Input } from './ui/input'

{/* START README */}
The `Sheet` component provides slide-out panels that appear from screen edges for secondary content, forms, or navigation. It offers an alternative to modals when you need to maintain context with the main content.

Use this component when you need to:
- Create mobile-friendly navigation menus
- Display detailed forms or settings panels
- Show additional information without full page navigation
- Provide contextual actions or filters for the main content

The sheet slides in from configurable directions (right, left, top, bottom), includes proper focus management, and provides structured sections for headers, content, and footers.
{/* END README */}

{/*
AI_DOCUMENTATION_START

Technical Overview: Shadcn UI re-export built on Radix UI's Dialog primitive with slide-in animations and portal rendering
Dependencies: @radix-ui/react-dialog, framer-motion or CSS transitions

Available Stories:
- Default: Right-side sheet with profile editing form including name, username, and save/cancel actions

Props (Sheet component):
- open: boolean - Controlled open state
- defaultOpen: boolean - Initial open state
- onOpenChange: (open: boolean) => void - Open state change handler
- modal: boolean - Whether sheet is modal (default: true)

Component Structure:
- Sheet: Root container managing sheet state (re-export from ui/sheet)
- SheetTrigger: Element that opens the sheet
- SheetContent: Slide-in content container with direction prop
- SheetHeader: Header section for title and description
- SheetTitle: Primary heading with proper typography
- SheetDescription: Optional subtitle or description
- SheetFooter: Footer section for actions
- SheetClose: Close button or trigger

Usage Examples:
```tsx
// Right-side settings sheet
<Sheet>
  <SheetTrigger asChild>
    <Button variant="outline">Settings</Button>
  </SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>Account Settings</SheetTitle>
      <SheetDescription>
        Update your account preferences and information.
      </SheetDescription>
    </SheetHeader>
    <div className="space-y-4 py-4">
      <div className="space-y-2">
        <Label htmlFor="name">Name</Label>
        <Input id="name" defaultValue="John Doe" />
      </div>
      <div className="space-y-2">
        <Label htmlFor="email">Email</Label>
        <Input id="email" defaultValue="john@example.com" />
      </div>
    </div>
    <SheetFooter>
      <SheetClose asChild>
        <Button type="submit">Save changes</Button>
      </SheetClose>
    </SheetFooter>
  </SheetContent>
</Sheet>

// Left-side navigation sheet
<Sheet>
  <SheetTrigger asChild>
    <Button variant="ghost" size="sm">
      ☰ Menu
    </Button>
  </SheetTrigger>
  <SheetContent side="left">
    <nav className="space-y-4">
      <a href="/dashboard" className="block p-2 rounded hover:bg-gray-100">
        Dashboard
      </a>
      <a href="/projects" className="block p-2 rounded hover:bg-gray-100">
        Projects
      </a>
      <a href="/settings" className="block p-2 rounded hover:bg-gray-100">
        Settings
      </a>
    </nav>
  </SheetContent>
</Sheet>

// Bottom sheet (mobile-style)
<Sheet>
  <SheetTrigger asChild>
    <Button>Share</Button>
  </SheetTrigger>
  <SheetContent side="bottom">
    <SheetHeader>
      <SheetTitle>Share this content</SheetTitle>
    </SheetHeader>
    <div className="grid grid-cols-3 gap-4 py-4">
      <Button variant="outline">Email</Button>
      <Button variant="outline">Twitter</Button>
      <Button variant="outline">Copy Link</Button>
    </div>
  </SheetContent>
</Sheet>
```

Related Components:
- Modal: For content requiring full attention
- Drawer: Alternative sliding panel implementation
- Sidebar: For persistent navigation panels
- Popover: For smaller contextual content

AI_DOCUMENTATION_END
*/}

export const Default = () => {
  return (
    <Sheet>
      <SheetTrigger asChild>
        <Button variant="outline">Open</Button>
      </SheetTrigger>
      <SheetContent>
        <SheetHeader>
          <SheetTitle>Edit profile</SheetTitle>
          <SheetDescription>
            Make changes to your profile here. Click save when you&apos;re done.
          </SheetDescription>
        </SheetHeader>
        <div className="grid flex-1 auto-rows-min gap-6 px-4">
          <div className="grid gap-3">
            <ShadcnLabel htmlFor="sheet-demo-name">Name</ShadcnLabel>
            <Input id="sheet-demo-name" defaultValue="Pedro Duarte" />
          </div>
          <div className="grid gap-3">
            <ShadcnLabel htmlFor="sheet-demo-username">Username</ShadcnLabel>
            <Input id="sheet-demo-username" defaultValue="@peduarte" />
          </div>
        </div>
        <SheetFooter>
          <Button type="submit">Save changes</Button>
          <SheetClose asChild>
            <Button variant="outline">Close</Button>
          </SheetClose>
        </SheetFooter>
      </SheetContent>
    </Sheet>
  )
}
