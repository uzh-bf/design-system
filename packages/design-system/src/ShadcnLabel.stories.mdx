import { ShadcnLabel } from './ShadcnLabel'
import { Checkbox } from './ui/checkbox'

{/* START README */}
The `Label` component currently represents a simple re-export of the Shadcn UI Label component. More information on the structure can be found on the [Shadcn UI Label documentation](https://ui.shadcn.com/docs/components/label).
{/* END README */}

export const Default = () => {
  return (
    <div>
      <div className="flex items-center space-x-2">
        <Checkbox id="terms" />
        <ShadcnLabel htmlFor="terms">Accept terms and conditions</ShadcnLabel>
      </div>
    </div>
  )
}
