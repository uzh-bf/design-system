import * as React from 'react'
import { ScrollArea } from './ScrollArea'
import { Separator } from './Separator'

{/* START README */}
<div className="prose prose-sm max-w-none">
The `ScrollArea` component provides custom-styled scrollable regions with consistent scrollbar appearance across different operating systems and browsers. It replaces default browser scrollbars with more aesthetically pleasing and customizable alternatives.

Use this component when you need to:

- Create scrollable content areas with consistent styling
- Control scrollbar appearance across different platforms
- Implement smooth scrolling behavior
- Add custom scrollbar styling to match your design system

The scroll area maintains native scrolling performance while providing visual consistency and supports both vertical and horizontal scrolling with customizable scrollbar appearance.
</div>
{/* END README */}

{/*
AI_DOCUMENTATION_START

Technical Overview: Shadcn UI re-export built on Radix UI's ScrollArea primitive with custom scrollbar styling
Dependencies: @radix-ui/react-scroll-area

Available Stories:

- Default: Vertical scroll area with tags list demonstrating styled scrollbars

Props (ScrollArea component):

- type: "auto" | "always" | "scroll" | "hover" - Scrollbar visibility behavior
- scrollHideDelay: number - Delay before hiding scrollbars (default: 600ms)
- dir: "ltr" | "rtl" - Text direction
- className: string - Additional CSS classes
- orientation: "vertical" | "horizontal" | "both" - Scroll directions

Component Structure:

- ScrollArea: Root container managing scroll behavior (re-export from ui/scroll-area)
- ScrollAreaViewport: Scrollable content viewport
- ScrollAreaScrollbar: Custom scrollbar element
- ScrollAreaThumb: Draggable scrollbar thumb
- ScrollAreaCorner: Corner element for bidirectional scrolling

Usage Examples:

```tsx
// Vertical scroll area
<ScrollArea className="h-72 w-48 rounded-md border">
  <div className="p-4">
    {items.map((item, index) => (
      <div key={index} className="text-sm">
        {item}
        {index < items.length - 1 && <Separator className="my-2" />}
      </div>
    ))}
  </div>
</ScrollArea>

// Horizontal scroll area
<ScrollArea className="w-96 whitespace-nowrap rounded-md border">
  <div className="flex w-max space-x-4 p-4">
    {images.map((image, index) => (
      <figure key={index} className="shrink-0">
        <img
          src={image.src}
          alt={image.alt}
          className="aspect-3/4 h-fit w-fit object-cover"
          width={300}
          height={400}
        />
      </figure>
    ))}
  </div>
  <ScrollBar orientation="horizontal" />
</ScrollArea>

// Chat message area
<ScrollArea className="h-80 w-full rounded-md border p-4">
  {messages.map((message, index) => (
    <div key={index} className="mb-4">
      <div className="font-semibold">{message.author}</div>
      <div className="text-sm text-muted-foreground">{message.content}</div>
    </div>
  ))}
</ScrollArea>
```

Related Components:

- Card: Often contains scroll areas for content
- Sheet: May include scroll areas for long content
- Table: Can use scroll areas for large datasets

AI_DOCUMENTATION_END
*/}

export const Default = () => {
  return (
    <ScrollArea className="h-72 w-48 rounded-md border">
      <div className="p-4">
        <h4 className="mb-4 text-sm leading-none font-medium">Tags</h4>
        {Array.from({ length: 50 })
          .map((_, i, a) => `v1.2.0-beta.${a.length - i}`)
          .map((tag) => (
            <React.Fragment key={tag}>
              <div className="text-sm">{tag}</div>
              <Separator className="my-2" />
            </React.Fragment>
          ))}
      </div>
    </ScrollArea>
  )
}
