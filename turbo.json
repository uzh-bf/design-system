{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "format": {},
    "format:check": {
      "outputMode": "errors-only"
    },
    "lint": {
      "outputMode": "errors-only"
    },
    "check": {
      "cache": false,
      "outputMode": "errors-only",
      "dependsOn": ["^build"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "dependsOn": []
    }
  }
}
